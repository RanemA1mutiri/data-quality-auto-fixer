# Light is the default. Only the accent + font are pinned here so that
# Streamlit's native Dark theme (⋮ → Settings → Theme) works fully — tables
# and all chrome included. Our custom surfaces follow via st.context.theme.
[theme]
base = "light"
primaryColor = "#4F46E5"
font = "sans serif"
